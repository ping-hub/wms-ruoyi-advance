<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.wms.mapper.WmsEnvMonitorDataMapper">

    <!-- 批量插入 -->
    <insert id="insertBatch">
        INSERT INTO wms_env_monitor_data
            (collect_id, device_id, device_type, online, collect_time, device_value, alar_status, receive_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.collectId},
                #{item.deviceId},
                #{item.deviceType},
                #{item.online},
                #{item.collectTime},
                #{item.deviceValue},
                #{item.alarStatus},
                NOW()
            )
        </foreach>
    </insert>

    <!-- 查询每个传感器的最新一条数据（按 collect_time 倒序，取每组首条） -->
    <select id="selectLatestByDevice" resultType="map">
        SELECT DISTINCT ON (device_id)
            device_id    AS "deviceId",
            collect_id   AS "collectId",
            device_type  AS "deviceType",
            online,
            collect_time AS "collectTime",
            device_value AS "deviceValue",
            alar_status  AS "alarStatus"
        FROM wms_env_monitor_data
        ORDER BY device_id, collect_time DESC
    </select>

</mapper>
